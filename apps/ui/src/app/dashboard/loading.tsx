export default function DashboardLoading() {
  return (
    <main className="shell">
      <section className="hero">
        <div>
          <p className="eyebrow">Loading GridFlex dashboard</p>
          <h1>Fetching scheduler state...</h1>
          <p className="lede">Connecting to the active GridFlex backend and preparing the latest demo view.</p>
        </div>
      </section>
    </main>
  );
}