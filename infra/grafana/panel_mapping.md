# Grafana Panel Mapping

- Jobs Total: `gridflex_jobs_total` as `stat`
- Jobs Shifted: `gridflex_jobs_shifted_total` as `stat`
- Jobs Admitted Now: `gridflex_jobs_admitted_now_total` as `stat`
- Shift Ratio: `gridflex_decision_shift_ratio` as `gauge`
- Peak kWh Avoided: `gridflex_peak_kwh_avoided` as `stat`
- GPU Utilisation Preserved: `gridflex_gpu_utilisation_preserved_pct` as `gauge`
- Mean Stress Before: `gridflex_grid_stress_before` as `stat`
- Mean Stress After: `gridflex_grid_stress_after` as `stat`
- Deadline Miss Rate: `gridflex_deadline_miss_rate` as `stat`
- Estimated Carbon Saving: `gridflex_estimated_carbon_saving_kgco2` as `stat`
- Active Payload Available: `gridflex_payload_available` as `stat`
- DGX Payload Available: `gridflex_dgx_payload_available` as `stat`
